{
  "name": "emergent",
  "private": true,
  "workspaces": [
    "app/frontend",
    "app/backend"
  ],
  "scripts": {
    "build": "npm --workspace app/frontend run build",
    "start": "npm run build && npm --workspace app/backend start",
    "dev": "npm --workspace app/frontend run dev"
  }
}
